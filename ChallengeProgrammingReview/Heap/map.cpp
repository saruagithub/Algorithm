//
//  map.cpp
//  test
//
//  Created by wangxue on 17/7/1.
//  Copyright © 2017年 wangxue. All rights reserved.
//

/*
#include <stdio.h>
#include <iostream>
#include <cstdio>
#include <string>
#include <map>
using namespace std;

int main(){
    map<int, const char*> m;
    m.insert(make_pair(1, "One"));
    m.insert(make_pair(2, "Two"));
    m[100] = "Hundred";
    
    map<int ,const char*>::iterator ite;
    ite = m.find(1);
    puts(ite->second);  //input one
    
    ite = m.find(2);
    if (ite == m.end()) {
        puts("Not found");
    }
    puts(m[10]);
    m.erase(10);
    for (ite=m.begin(); ite!=m.end(); ++ite) {
        printf("%d: %s\n",ite->first,ite->second);
    }
    
    return 0;
}*/
