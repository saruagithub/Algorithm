//
//  Set.cpp
//  test
//
//  Created by wangxue on 17/6/19.
//  Copyright © 2017年 wangxue. All rights reserved.
//

/*
#include <stdio.h>
#include <cstdio>
#include <set>
using namespace std;

int main()
{
    set<int> s;
    s.insert(1);
    s.insert(2);
    s.insert(0);
    s.insert(3);
    s.insert(6);
    s.insert(4);
    set<int>::iterator ite;
    ite = s.find(1);
    if(ite == s.end())
        puts("Not find!");
    else
        puts("Found");
    
    ite = s.find(2);
    if(ite == s.end())
        puts("Not find!");
    else
        puts("Found");
    
    for(ite=s.begin();ite!=s.end();++ite){
        printf("%d ",*ite);
    }
    return 0;
}
*/
